<%= form_with(model: opu, local: true) do |form| %>
  <% if opu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opu.errors.count, "error") %> prohibited this opu from being saved:</h2>

      <ul>
      <% opu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :designer %>
    <%= form.text_field :designer %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
